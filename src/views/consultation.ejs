<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consultation - Frenos Hugo</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Consultation of Services</h1>
    </header>
    <main>
        <form action="/consultation" method="GET">
            <label for="licensePlate">Enter License Plate:</label>
            <input type="text" id="licensePlate" name="licensePlate" required>
            <button type="submit">Consult</button>
        </form>
        <% if (services && services.length > 0) { %>
            <h2>Services Performed:</h2>
            <ul>
                <% services.forEach(service => { %>
                    <li>
                        <strong>Completed Work:</strong> <%= service.completedWork %> - 
                        <strong>Cost:</strong> $<%= service.cost %>
                    </li>
                <% }); %>
            </ul>
        <% } else if (services) { %>
            <p>No services found for this license plate.</p>
        <% } %>
    </main>
    <footer>
        <p>&copy; 2023 Frenos Hugo. All rights reserved.</p>
    </footer>
</body>
</html>