# Configuración local de desarrollo
# DATABASE_URL=postgresql://username:password@localhost:5432/frenos_hugo

# En Render, esta variable se configurará automáticamente cuando agregues PostgreSQL
# DATABASE_URL será proporcionada por Render automáticamente

# Configuración del servidor
NODE_ENV=development
PORT=3000
